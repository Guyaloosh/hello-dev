apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-dev.fullname" . }}
  labels:
    {{- include "hello-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-dev.selectorLabels" . | nindent 4 }}
