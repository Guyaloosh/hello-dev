apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-dev.fullname" . }}-test-connection"
  labels:
    {{- include "hello-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
